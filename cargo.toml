[workspace]
members = ["ipc_host", "xplane_uipc", "xtask", "uipc-expr", "uipc-mapping", "uipc-debug"]
resolver = "3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
panic = "abort"
